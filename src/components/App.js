import React from "react";
import Counter from "./Counter"; // Importer le composant Counter

const App = () => {
  return (
    <div id="root">
      <Counter /> {/* Afficher le composant Counter */}
    </div>
  );
};

export default App;
